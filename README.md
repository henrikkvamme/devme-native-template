# Devme Native Template

Native iOS and Android starter with a self-hosted Convex backend and Devme.

## Start

Requires Xcode, Docker, Bun, and Devme.

```sh
devme up -d --wait
devme run backend-deploy --output toon
devme
```

Development only. Add authentication and replace the Sambu sample identifiers before shipping.
